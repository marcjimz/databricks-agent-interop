# Production Environment Overrides
# Override base gateway.yaml settings for production environment

gateway:
  environment: "prod"

azure:
  location: "eastus2"
  resource_prefix: "a2a"

apim:
  sku: "Standard"  # Production-grade SLA

tracing:
  enabled: true
  log_level: "warning"  # Less verbose in production
