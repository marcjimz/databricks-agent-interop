# Databricks Agent Interoperability Framework
# Copy this file to .env and fill in your values

# =============================================================================
# Azure / Entra ID (required for infrastructure deployment)
# =============================================================================
TENANT_ID=your-azure-tenant-id
SUBSCRIPTION_ID=your-azure-subscription-id
LOCATION=westus

# =============================================================================
# Infrastructure naming
# =============================================================================
PREFIX=mcpagent01
RESOURCE_GROUP=rg-mcpagent01

# =============================================================================
# Databricks (set automatically after 'make deploy-infra')
# =============================================================================
# DATABRICKS_HOST=https://adb-xxxx.azuredatabricks.net

# =============================================================================
# Unity Catalog
# =============================================================================
UC_CATALOG=mcp_agents
UC_SCHEMA=tools

# =============================================================================
# Azure AI Foundry (set automatically after 'make deploy-infra')
# =============================================================================
# FOUNDRY_ENDPOINT=https://your-foundry.cognitiveservices.azure.com

# =============================================================================
# Azure AI Foundry Agent Service (for 'make deploy-foundry-agent')
# IMPORTANT: This requires a STANDALONE Foundry project (not hub-based).
# Hub-based projects created via Terraform are NOT supported by the Agent SDK.
#
# To create a standalone project:
# 1. Go to https://ai.azure.com and click "New project"
# 2. Create a standalone project (do NOT connect to an existing hub)
# 3. Deploy a model (e.g., gpt-4o)
# 4. Copy endpoint from: Libraries > Foundry > Overview
# =============================================================================
# PROJECT_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
